# missing_data_report
A REDCap module that generates a report of missing data by form. It displays record ID, event, arm, repeat instrument, instance, and form name, along with a count of missing fields and a comma-separated list of those fields. The report is filterable and exportable as CSV
